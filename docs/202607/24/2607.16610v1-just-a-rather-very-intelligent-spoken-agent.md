---
title: Just A Rather Very Intelligent Spoken Agent
title_zh: 一个相当非常智能的语音代理
authors: "Chen Chen, Zhehuai Chen"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16610v1"
tags: ["query:hai"]
score: 8.0
evidence: 始终在线的语音中介支持人机实时协作
tldr: 当前长时域AI代理与用户交互薄弱，缺乏始终在线的语音调解层。本文提出JarvisBench基准，包含代理协作和用户交互两个轨道，用于衡量调解层的双重价值。基于模块化原型在34个WildClaw任务上的评估表明，Jarvis式调解能提供可追溯的回答，并在适当时机注入用户指导提升任务性能。效果依赖于调解器的大模型大脑，突显了中间层的重要性和社区研究必要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16610v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1426, \"height\": 761, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16610v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 476, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16610v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1434, \"height\": 273, \"label\": \"Table\"}]"
motivation: 现有长时域AI代理与用户交互薄弱，缺少实时语音调解层以增强可理解性和响应性。
method: 提出JarvisBench基准，含代理协作和用户交互两轨道，构建模块化Jarvis原型并评估其在WildClaw任务上的表现。
result: Jarvis式调解可提供基于执行轨迹的回答，注入稀疏用户指导能提升任务性能，效果取决于调解器LLM能力。
conclusion: Jarvis式调解层能改善代理与用户交互，但依赖大模型能力，需社区共同努力推进。
---

## 摘要
长周期AI代理正变得越来越强大，但它们与用户的交互仍然出奇地单薄。在大多数工作流程中，用户给出初始指令后，只能收到选择性的文本更新，并且对代理正在做什么或何时介入缺乏清晰的认识。这导致当前代理生态系统缺少了一个部分：一个始终在线的贾维斯式中介，使代理能够持续与用户保持联系。这样的中介应支持与用户的实时语音交互，在不中断工作进程的情况下回答问题，主动报告进展或困惑，并在有用时将用户指导注入回代理的执行中。在这项工作中，我们引入了JarvisBench，这是一个用于衡量长周期代理工作流程中中介双重价值的基准。JarvisBench包含两个互补的轨道：一个代理协作轨道，衡量中介是否改善了下游任务完成情况；一个用户交互轨道，衡量中介是否使正在进行的执行对用户更可理解、响应更及时、更易访问。我们用一个模块化的参考Jarvis原型实例化该基准，并在OpenClaw中执行的34个纯文本WildClaw任务上进行了评估。使用GPT-5.5、Claude Opus 4.7、基于Gemini和基于GPT的工作代理的初步结果表明，贾维斯式的中介可以为用户问题提供基于轨迹的响应，并在适当时刻注入稀疏用户指导时提高任务性能。结果还表明，有效性在很大程度上取决于中介的LLM大脑，突显了这个缺失中间层的潜力以及需要更广泛的社区努力。演示页面 https://cchen1436.github.io/jarvis

## Abstract
Long-horizon AI agents are becoming increasingly capable, yet their interaction with users remains surprisingly thin. In most workflows, users give an initial instruction, receive only selective textual updates, and lose a clear sense of what the agent is doing or when to step in. This leaves a missing part in the current agent ecosystem: an always-on Jarvis-style mediator that keeps the agent continuously reachable to the user. Such a mediator should support real-time spoken interaction with the user, answer questions without interrupting the worker, proactively report progress or confusion, and inject user guidance back into the agent's execution when useful. In this work, we introduce JarvisBench, a benchmark for measuring the dual value of mediation in long-horizon agent workflows. JarvisBench contains two complementary tracks: an agent-collaboration track that measures whether mediation improves downstream task completion, and a user-interaction track that measures whether mediation makes ongoing execution more understandable, responsive, and accessible to users. We instantiate the benchmark with a modular reference Jarvis prototype and evaluate it on 34 text-only WildClaw tasks executed in OpenClaw. Preliminary results with GPT-5.5, Claude Opus 4.7, Gemini-based, and GPT-based worker agents suggest that Jarvis-style mediation can provide trace-grounded responses to user questions and improve task performance when sparse user guidance is injected at appropriate moments. The results also show that effectiveness depends strongly on the mediator's LLM brain, highlighting both the promise of this missing middle layer and the need for broader community effort. Demo page https://cchen1436.github.io/jarvis

---

## 论文详细总结（自动生成）

## 论文结构化总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：当前长周期AI代理（如执行复杂、多步骤任务的智能体）虽然能力不断增强，但与用户的交互模式仍然薄弱。用户只能通过初始指令和少数文本更新了解进展，缺乏实时的、沉浸式的沟通渠道。这种“薄交互”导致用户难以理解代理在做什么、何时遇到困难或偏离意图，代理也无法在关键节点获得人类及时指导。
- **整体含义**：论文指出现有代理生态系统中缺失了一个“始终在线的语音调解层”——类似科幻电影中的贾维斯（J.A.R.V.I.S.），它既能向用户自然报告进展、回答问题，又能将用户的简洁反馈注入代理执行过程，从而提升任务成功率与用户透明性。作者将此缺失层转化为一个可基准测试的问题，并提出JarvisBench来量化其价值。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出一个可插拔的调解器（mediator）模型，置于用户与工作代理（worker agent）之间，通过实时口语交互与执行轨迹监控实现双向沟通。
- **关键技术细节**：
  - **JarvisBench基准**：分为两个轨道：
    - **代理协作轨道（Agent-Collaboration Track）**：评估调解器能否通过监控工作代理的执行轨迹（如ReAct轨迹），在失败敏感节点（如重复工具失败、进展停滞、模糊观察、高风险操作、不自信的终结）触发检查点，向专家用户请求简洁指导并注入回工作代理上下文。指标包括总体得分、相对增益、满分任务数、干预次数。
    - **用户交互轨道（User-Interaction Track）**：评估调解器能否在非专家用户提问时（进度类问题、主题类问题）提供基于执行轨迹的及时、准确的回答。指标包括进度回答质量（1-5分）、主题回答质量、回答延迟。
  - **模块化参考原型Jarvis**：
    - 用户侧：级联全双工语音管道，含流式ASR、语义VAD（SoulX-Duplug）、流式TTS（Chatterbox）。
    - 代理侧：接收工作代理的文本ReAct更新，维护执行上下文和对话上下文，执行交互检查点（合作暂停机制）。
    - 调解器不替代工作代理执行任务，仅负责监控、报告和注入用户指导。
- **算法流程文字说明**：
  1. 工作代理在OpenClaw环境中执行任务，持续输出ReAct轨迹。
  2. 调解器（Jarvis）监控轨迹，识别失败敏感点（基于预定义规则或LLM判断）。
  3. 在代理协作轨道：触发检查点→用简洁摘要向专家用户提问→用户给出简短修正建议→Jarvis将建议注入工作代理上下文。
  4. 在用户交互轨道：预定义检查点或用户主动提问→Jarvis基于当前执行轨迹回答进度/主题问题→继续执行。

### 3. 实验设计：数据集、场景、基准、对比方法
- **数据集与场景**：使用WildClawBench中的34个纯文本任务，涵盖生产力流程、代码情报、社交互动、搜索检索、创意合成、安全对齐六大类。任务需要多步规划、工具编排、错误恢复、来源核对和安全决策。运行环境为OpenClaw或Hermes平台。
- **基准**：以无调解的原始OpenClaw执行为基线。
- **对比方法**：
  - 代理协作轨道：将不同工作代理（GPT-5.4-mini, GPT-5.5, Gemini-3.1-Pro, Claude Opus 4.7）在有无Jarvis调解下对比。固定Jarvis大脑为GPT-5.4，专家用户模拟器也由GPT-5.4实现。
  - 用户交互轨道：固定工作代理为GPT-5.4-mini，变化Jarvis大脑（Qwen3-30B-A3B-Instruct, Qwen3-32B-Thinking, GPT-OSS-120B, GPT-5.4-mini, GPT-5.4），评估用户交互质量。
- **评判协议**：任务完成度按WildClaw官方协议；用户回答质量由LLM裁判（GPT-5.4）对进度和主题分别评分（1-5）。

### 4. 资源与算力
- **文中未明确说明**：论文未提及训练模型的计算资源（如GPU型号、数量、训练时长）。仅提到使用了GPT系列、Claude、Gemini等现有商业/开源模型作为调解器或工作代理，这些模型本身由外部开发。Jarvis原型运行时仅涉及推理，但具体硬件配置未描述。实验仅在34个任务上运行，未透露总推理耗时或租金成本。

### 5. 实验数量与充分性
- **实验数量**：
  - 代理协作轨道：4种工作代理 ×（有/无Jarvis）= 8组实验，报告了总体得分、增益、满分任务数、干预次数（共8行数据）。
  - 用户交互轨道：5种Jarvis大脑 × 固定工作代理 = 5组实验，报告了延迟、进度得分、主题得分等指标（共5行数据）。
  - 定性案例分析：附录给出了多个成功干预案例和模型失败模式分析。
- **充分性评估**：
  - **优点**：覆盖了多种主流LLM（闭源与开源），从效果和延迟两个维度比较；任务选择有实际意义；用户模拟器可复现；定性案例提供了深度理解。
  - **局限性**：
    - 任务数量仅34个，样本量较小，统计显著性未报告。
    - 仅使用单个固定用户模拟器（GPT-5.4），未比较不同模拟器的影响。
    - 没有报告多次运行的平均值和方差，单次结果可能受随机性影响。
    - 未进行交叉验证或消融实验（如不同的检查点策略、不同的VAD配置、不同的提示设计）。
    - 用户交互轨道中回答质量主观评分依赖单一LLM裁判，可能带有模型偏好。
  - **总体评价**：作为初步结果（preliminary results），实验设计合理但保守，充分性一般，需更大规模、更多重复来证实。

### 6. 论文的主要结论与发现
- **代理协作轨道**：
  - Jarvis调解在所有工作代理上均提升任务总体得分（相对增益1.6%至11.78%不等）。
  - Claude Opus 4.7基础最好（64.01%），经过Jarvis后提升至75.79%（∆=11.78%）；Gemini-3.1-Pro提升最显著（43.14%→54.78%）。
  - 干预次数很少（GPT-5.4-mini 6次，Claude Opus 18次），说明提升不是靠替代工作代理，而是通过识别高杠杆失败点并注入简洁修正。
  - 定性案例展示了Jarvis能帮助恢复工具失败、防止破坏性覆盖、强制满足隐藏约束等。
- **用户交互轨道**：
  - GPT-5.4作为Jarvis大脑在主题回答质量上最佳（3.91/5，73.9%≥4），GPT-5.4-mini在进度回答上接近（4.02/5），GPT-OSS-120B进度得分最高（4.07/5）。
  - Qwen模型延迟低但回答质量显著较差，尤其在主题问题上表现不佳。
  - 揭示调解器大脑对交互质量影响巨大，不仅是语音接口问题。
- **跨模型模式**：最困难的失败点不是常规推理，而是需要暂停并验证精确来源、输出格式、约束条件、文件状态和安全性等。调解器可在这些节点提供价值。

### 7. 优点：方法或实验设计上的亮点
- **问题定位新颖**：精准识别了当前AI代理社区忽略的“缺失中间层”，并给出系统化基准，而非仅提出一个特定模型。
- **双轨设计**：同时衡量对用户透明性和任务完成的改善，提供了更全面的价值评估。
- **可插拔评估协议**：允许独立替换调解器组件（LLM大脑、语音栈、监控策略、工作代理），便于社区贡献和对比。
- **模块化原型**：参考实现清晰划分对话上下文与执行上下文，使用级联语音管道保证可控性。
- **使用现实环境与任务**：WildClawBench基于真实工具（浏览器、shell、文件系统、邮件、日历），任务贴近实际场景。
- **用户模拟器**：通过LLM模拟非专家和专家用户，保证了实验可复现性和可扩展性。
- **定性案例**：附录提供了具体干预示例和模型失败模式分析，加深对结果的理解。

### 8. 不足与局限
- **实验覆盖有限**：仅34个任务，且排除了多模态任务，可能无法代表所有长周期代理场景。
- **缺乏统计显著性分析**：未报告置信区间或多次运行结果，无法判断增益是否显著。
- **仅使用单一用户模拟器**：用户行为被简化为GPT-5.4生成的问题/指导，未模拟真实用户的多样性（如不同口音、语速、中断模式）。
- **评估偏见**：用户交互轨道中回答质量由LLM裁判评估，可能偏向与裁判本身相似的模型风格。
- **缺乏消融研究**：没有分解每个组件贡献（如不同检查点策略、不同VAD算法、不同提示设计）。
- **延迟测量粗糙**：仅报告了从语音结束到回复开始的延迟，未分解ASR、推理、TTS各阶段耗时。
- **算力信息缺失**：无法评估实际部署成本或可扩展性。
- **应用限制**：当前原型依赖级联语音管道（ASR→VAD→LLM→TTS），延迟和错误累积可能影响用户体验；会话管理中未处理复杂中断（如用户插入关键指令的同时代理正在执行危险操作）。
- **伦理与安全**：论文提到安全对齐任务，但未讨论Jarvis自身被滥用或恶意注入的风险。

（完）
