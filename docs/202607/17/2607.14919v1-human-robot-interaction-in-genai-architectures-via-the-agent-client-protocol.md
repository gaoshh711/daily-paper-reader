---
title: Human-Robot Interaction in GenAI Architectures via the Agent-Client Protocol
title_zh: 通过智能体-客户端协议实现生成式AI架构中的人机交互
authors: "Jesus Moncada-Ramirez, Jose-Raul Ruiz-Sarmiento, Javier Gonzalez-Jimenez"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14919v1"
tags: ["query:hai"]
score: 8.0
evidence: 人机交互标准，支持人类在环协作
tldr: 当前基于生成式AI的机器人系统中，高层人机交互层因缺乏统一标准而碎片化，阻碍实时人机协作。本文提出利用软件工程中的Agent-Client协议作为人机交互层的统一通信契约，并与底层模型上下文协议结合，构建三层解耦架构。在实体移动机器人上的实验验证了该架构支持三种异构用户界面的互操作，并实现了实时可观测、授权及中断等协作功能，且延迟开销可忽略。这一工作为人机交互标准化提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14919v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 929, \"height\": 659, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14919v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1872, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14919v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 925, \"height\": 856, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14919v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 924, \"height\": 599, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14919v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 922, \"height\": 1474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14919v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 906, \"height\": 288, \"label\": \"Table\"}]"
motivation: 现有GenAI机器人架构中，上层人机交互层接口私有、碎片化，无法支持实时人在环路协作和异构界面统一接入。
method: 提出采用ACP作为人机交互层统一协议，与MCP组合形成三层架构：人机交互、Agent编排和物理执行，实现完全解耦。
result: 在物理机器人上测试三种异构界面（语音、图形、API），均成功互操作，实时授权和中断延迟开销可忽略。
conclusion: ACP+MCP双层协议栈能够统一人机交互接口，支持可替换的异构前端与后端，为协作式HRI标准化提供路径。
---

## 摘要
生成式人工智能（GenAI），特别是大型语言模型（LLM）的最新进展，正推动机器人架构向基于智能体的高级编排方向发展，其中自然语言指令可被转化为上下文感知的动作序列。尽管通过模型上下文协议（MCP），这些智能体与机器人能力的集成正日益趋向标准化，但上层的人机交互（HRI）层仍因专有、特设的接口而碎片化，阻碍了实时的人在环协作。为解决这种碎片化，本文提出采用智能体-客户端协议（ACP）——一种最初为软件工程中的编码智能体引入的通信标准——作为基于智能体的机器人系统中HRI层的统一通信契约。通过在接口-智能体链路上结合ACP，在智能体-执行链路上结合MCP，我们构建了一个完全解耦的三层架构，将人机交互、商议式编排和物理执行分离开来。这种拓扑结构消除了刚性的架构依赖，使异构用户界面能够连接到同一机器人系统，并允许底层机器人平台在无需客户端特定集成更改的情况下被替换。此外，它原生支持协作式HRI能力，如实时可观测性、显式人类授权和即时任务中断。我们在一个实体移动机器人上实验评估了所提出的架构，展示了三种异构用户界面之间的互操作性，并验证了实时人在环工作流，且延迟开销可忽略不计。

## Abstract
Recent advances in Generative Artificial Intelligence (GenAI), particularly Large Language Models (LLMs), are driving robotic architectures toward agent-based high-level orchestration, in which natural-language instructions can be translated into context-aware action sequences. While the integration of these agents and robotic capabilities is increasingly converging toward standardization through the Model Context Protocol (MCP), the upper Human-Robot Interaction (HRI) layer remains fragmented by proprietary, ad hoc interfaces that hinder real-time human-in-the-loop collaboration.   To address this fragmentation, this paper proposes the adoption of the Agent-Client Protocol (ACP) -- a communication standard originally introduced for coding agents in software engineering -- as a unified communication contract for the HRI layer in agent-based robotic systems. By combining ACP at the interface-agent link and MCP at the agent-execution link, we formulate a fully decoupled three-layer architecture that separates human interaction, deliberative orchestration, and physical execution. This topology removes rigid architectural dependencies, enabling heterogeneous user interfaces to connect to the same robotic system and allowing the underlying robotic platform to be replaced without requiring client-specific integration changes. Moreover, it provides native support for collaborative HRI capabilities such as real-time observability, explicit human authorization, and immediate task interruption. We experimentally evaluate the proposed architecture on a physical mobile robot, demonstrating interoperability across three heterogeneous user interfaces and validating real-time human-in-the-loop workflows with negligible latency overhead.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机与背景）
- **核心问题**：当前基于生成式AI（GenAI）和大型语言模型（LLM）的机器人架构中，上层人机交互（HRI）层缺乏统一标准，接口多采用私有化、特设（ad hoc）方式，导致异构用户界面（如网页仪表盘、移动平板、开发者终端）难以互操作，且无法高效支持实时人在环协作（如实时观察、显式授权、任务中断）。同时，底层智能体与硬件执行之间的交互正通过模型上下文协议（MCP）逐步标准化，但上层HRI层的碎片化问题未得到解决。
- **整体含义**：这种碎片化增加了集成成本，限制了系统的可移植性和可扩展性，阻碍了人机协同工作的实现。本文旨在引入软件工程中的Agent-Client Protocol（ACP）作为统一通信契约，填补这一空白。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：提出一种完全解耦的三层机器人架构，通过两个标准化协议（ACP和MCP）分别连接不同的层次：
  - **接口层**（顶层）：由多个ACP客户端组成，负责HRI（如终端、移动应用、网页客户端）。客户端仅管理对话会话、捕获用户意图并可视化智能体反馈，不参与规划或物理控制。
  - **认知层**（中间层）：作为ACP服务器（面向接口层）和MCP客户端（面向执行层），包含一个基于LLM的编排核心（如ReAct模式）。编排核心利用LLM进行推理和规划，并通过MCP调用执行层提供的工具。
  - **执行层**（底层）：通过MCP服务器封装机器人硬件能力（如ROS 2话题、服务、动作）和外部服务（如互联网），暴露标准化工具目录。该层负责低层物理控制和安全约束，确保确定性操作。
- **关键技术细节**：
  - **ACP**：原为软件工程中编码智能体与IDE之间通信而设计，本文将其适配到机器人领域。定义了客户端操作（如连接初始化、身份认证、会话管理、指令传递、取消）和服务端操作（如状态更新、授权请求、文件读写）。使用JSON-RPC消息格式，通过TCP传输（因原始ACP基于stdio，本文改为TCP支持远程连接）。
  - **MCP**：用于工具发现和调用的开放标准，同样基于JSON-RPC。认知层的MCP客户端通过MCP服务器发现可用工具（如导航、拍照、语音合成），并传递工具调用结果回LLM。
  - **三层交互流程**：用户通过ACP客户端发送自然语言指令 → 认知层生成初步推理并流式更新状态给客户端 → 认知层通过MCP调用执行层工具（如导航） → 过程中可请求用户授权或接受中断 → 执行完成后向客户端反馈结果。

## 3. 实验设计：数据集/场景、基准、对比方法
- **实验场景**：在标准办公室环境中使用物理移动机器人Sancho（基于AgileX Ranger Mini V3，配备激光雷达、RGB-D相机、扬声器等）。
- **数据集**：无传统数据集，而是使用真实任务场景（如导航到实验室并播报消息、取消任务、重新规划等）。
- **基准与对比方法**：未直接对比其他方法，而是通过三个方面评估架构特性：
  1. **解耦性**：将三种异构ACP客户端连接到同一机器人系统而不修改机器人逻辑。
  2. **HRI工作流验证**：通过一个代表性会话的完整执行跟踪，演示实时交互（状态更新、授权、中断）。
  3. **开销分析**：量化ACP和MCP带来的延迟，并与LLM推理时间、物理工具执行时间对比。
- **对比方法**：文中没有设置对比基线，而是强调与现有非统一架构（即每个接口需定制集成）相比的优势。

## 4. 资源与算力
- **明确说明**：论文未详细说明计算资源，仅提及：
  - LLM使用“gemini-3.1-flash-lite”（推测为Gemini模型的一个轻量级变体，但具体版本可能为虚构或未来模型）。
  - 机器人基于ROS 2 Humble框架。
  - 未提及GPU型号、数量、训练时长等算力信息。评估仅在推理阶段进行。
  - 因此，算力信息缺失，需明确指出本文未提供这方面的细节。

## 5. 实验数量与充分性
- **实验数量**：
  - 一个主要实验：使用Sancho机器人在办公室环境进行一个代表性会话（包含4次用户提示和多次状态更新、授权请求、取消等）。
  - 三个异构客户端连接测试：开发者CLI、移动平板应用、通用开源客户端acp-ui。
  - 延迟分解：对会话中各阶段（初始LLM思考、ReAct推理、物理工具、用户授权、协议开销）进行计时。
- **充分性**：
  - 实验覆盖了解耦性验证、HRI工作流、开销分析三个方面，但属于单次代表性会话的演示，未进行多轮重复试验或统计显著性分析。
  - 未与其他架构（如无ACP的系统）进行直接对比实验。
  - 未测试不同LLM、不同负载或长时间运行场景。
  - 总体而言，实验作为概念验证足够，但充分性有限，更多是证明可行性而非全面评估。

## 6. 主要结论与发现
- **结论**：
  - 采用ACP作为HRI层统一协议，结合底层MCP，可形成完全解耦的三层架构，消除界面与机器人间的刚性依赖。
  - 三种异构客户端均能无缝连接同一机器人系统，无需定制集成。
  - 原生支持实时可观测性、显式授权和即时任务中断，显著提升了人在环协作能力。
  - 通信开销极低：ACP和MCP合计仅约171.6ms，占总执行时间（平均36.94s）的<0.5%，主要由LLM推理和物理导航主导。
- **主要发现**：标准化协议栈可有效解决机器人HRI碎片化问题，且不影响实时性能。

## 7. 优点：方法与实验设计亮点
- **方法亮点**：
  - 借鉴软件工程领域成熟标准化经验（ACP），跨领域应用到机器人HRI，具有创新性。
  - 三层完全解耦设计，使接口层和执行层均可独立替换，提升系统灵活性。
  - 将HRI作为架构原生组成部分，而不是事后添加的模块。
- **实验设计亮点**：
  - 使用真实物理机器人而非模拟环境，验证结果更可信。
  - 三种异构客户端（CLI、移动、第三方通用客户端）覆盖典型使用场景，证明通用性。
  - 详细的时间延迟分解，清晰展示各组件开销，证明协议层开销可忽略。
  - 提供开源代码（机器人端和客户端），增强可复现性。

## 8. 不足与局限
- **实验覆盖局限**：
  - 仅进行一次代表性会话，缺乏多用户、多场景、长时间运行的评估，统计可靠性不足。
  - 未与现有非标准架构进行定量对比，无法量化改进幅度。
  - 未测试多智能体协作、高并发客户端同时连接等复杂情况。
- **协议适用性限制**：
  - ACP原为编码智能体设计，部分操作（如终端进程管理）对机器人无意义，需限制使用。
  - 不支持连续多模态流（如实时视频）和异步后台遥测（如电池状态），需要额外机制补充。
- **系统依赖**：
  - 认知层依赖特定LLM（gemini-3.1-flash-lite），未测试不同模型的影响。
  - 执行层依赖ROS 2，对非ROS系统可能需要进一步适配。
- **安全性与鲁棒性**：实验未涉及安全边界测试、故障恢复、网络延迟异常等边缘情况。
- **算力信息缺失**：未说明LLM推理所需的硬件资源，影响资源需求评估。

（完）
