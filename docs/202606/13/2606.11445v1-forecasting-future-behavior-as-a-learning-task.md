---
title: Forecasting Future Behavior as a Learning Task
title_zh: 将未来行为预测作为学习任务
authors: "Mosh Levy, Yoav Goldberg, Asa Cooper Stickland"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11445v1"
tags: ["query:hai"]
score: 8.0
evidence: 提出学习预测AI行为作为解释的替代方案以建立信任
tldr: 针对大型推理模型行为难以通过解释预测的问题，本文提出将行为预测视为可学习任务，训练行为预测器直接基于单条推理轨迹进行预测。该方法无需人工标注，仅需一次前向传播。在重复答案和输入移除两个任务上，训练后的预测器准确率超过GPT-5.4和Claude Opus-4.6，且推理成本极低。结果表明推理轨迹蕴含超越朴素阅读的丰富信息。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11445v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1355, \"height\": 599, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11445v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1446, \"height\": 662, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11445v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1085, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1237, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 927, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1247, \"height\": 392, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 746, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 836, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 663, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 592, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1165, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1447, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1275, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1400, \"height\": 177, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11445v1/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 1448, \"height\": 199, \"label\": \"Table\"}]"
motivation: 现有解释方法难以泛化到长推理轨迹，且轨迹作为自然语言阅读时往往不忠实，故需绕过解释步骤直接预测未来行为。
method: 收集LLM的推理轨迹作为训练数据，无需人工标注；训练Behavior Forecaster网络，以单条推理轨迹为输入，预测未来行为。
result: 在三个推理数据集上，训练后的预测器准确率超过GPT-5.4和Claude Opus-4.6，且推理成本仅为后者的极小部分。
conclusion: 推理轨迹包含关于LLM未来行为的丰富信息，通过学习可更高效准确地进行行为预测，为理解大型推理模型提供新范式。
---

## 摘要
对AI系统的信任通常基于对其工作方式的解释，然后利用这些解释来预测系统在新输入上的行为。对于大型推理模型（LRM），这条传统路径尤其难以遵循：针对单token生成的解释方法无法自然地推广到长轨迹，而轨迹本身在作为自然语言阅读时往往不够忠实。我们提出了一种替代方案，绕过了解释步骤：将行为预测视为一个可学习的任务，并训练行为预测器，该预测器基于单条推理轨迹做出通常需要从解释中获得的相同预测。预测器的训练数据通过查询LRM获得，无需人工标注，其推理通过单次前向传递完成。我们在两个任务上实例化了该方法：LRM在重复运行时重复其答案的可能性，以及删除输入部分如何改变其答案。我们在三个不同的推理数据集上对这两个任务评估了这一方法，发现训练后的行为预测器比GPT-5.4和Claude Opus-4.6作为朴素读者阅读相同轨迹时更准确，而其推理成本仅为其一小部分。我们发现，端到端微调主干网络并使其从目标LRM初始化，对于获得强性能都是必需的。这些结果表明，推理轨迹携带了关于LRM未来行为的信息，超越了朴素阅读所能传达的信息。

## Abstract
Trust in an AI system is often anchored by explanations of how it works, which one then uses to forecast its behavior on new inputs. For large reasoning models (LRMs), this conventional route is particularly difficult to follow: explanation methods for single token generations do not naturally generalize to long trajectories, and the trajectories themselves are often not faithful when read as natural language. We propose an alternative that bypasses the explanation step: treat behavior forecasting as a learnable task and train Behavior Forecasters that operates on a single reasoning trajectory to make the same forecasts one would typically seek from an explanation. The forecaster's training data is obtained by querying the LRM with no human annotation, and its inference is done in a single forward pass. We instantiate this approach on two tasks: how likely the LRM is to repeat its answer on re-runs, and how removing parts of the input changes its answer. We evaluate this approach on both tasks across three diverse reasoning datasets and find that trained Behavior Forecasters are more accurate than GPT-5.4 and Claude Opus-4.6 reading the same trajectories as naive readers, at a small fraction of their inference cost. We find that fine-tuning the backbone end-to-end and initializing it from the target LRM are each necessary for strong performance. These results show that the reasoning trajectory carries information about the LRM's future behavior that goes beyond what naive reading conveys.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **信任困境**：对AI系统的信任依赖于对其未来行为的预测能力。传统上，这种预测通过解释模型如何工作来实现（如特征归因、注意力等）。但对于**大型推理模型（LRM）**（如o1、R1），现有解释方法存在根本缺陷：
  - 单token生成解释无法自然推广到长链推理轨迹；
  - 推理轨迹作为自然语言阅读时往往**不忠实**——模型可能遗漏关键因素、产生语义错位，甚至与真实计算过程不一致。
- **核心问题**：如何在不依赖不忠实解释的前提下，高效、准确地预测LRM在特定输入上的未来行为（如重跑一致性、反事实敏感性）。
- **整体含义**：论文提出**绕过解释步骤**，将行为预测本身视为一个可学习的任务。认为**推理轨迹中隐含着关于未来行为的信息，但不需要通过人类可读的自然语言来提取**，而是可以通过训练一个专门的模型（行为预测器）来挖掘这些模式，从而建立对LRM的信任。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）
- **核心思想**：训练一个**行为预测器（Behavior Forecaster）**，以**单条推理轨迹（τ = prompt, reasoning, answer）为输入**，直接预测目标LRM在该输入上的未来行为属性（如重跑一致性、反事实敏感性）。预测器本身是一个轻量级模型，**推理只需一次前向传播**，远快于重新采样或使用大型LLM阅读。
- **关键技术细节**：
  - **训练数据自动生成**：通过对目标LRM进行多次采样（10次重跑、10次反事实扰动）获得标签，无需人工标注。
  - **架构**：主干网络与目标LRM**同架构**，并**初始化自目标LRM的权重**（如OLMo-3-7B-Think）。然后端到端微调，附加任务特定头部：
    - **反事实敏感性任务**：采用`P–R–A–P`（提示-推理-答案-提示副本）排列，在副本提示的每个位置用MLP输出分数，再聚合到段落级标签。损失为二元交叉熵。
    - **重跑一致性任务**：采用`P–R–A`排列，通过交叉注意力池化（learned queries）将变长序列压缩为固定向量，再用MLP输出一个标量。损失为均方误差。
  - **算法流程**：对于每个输入P，获取一条轨迹τ → 行为预测器前向传播 → 输出预测值（一个标量或每段分数）。训练时，使用τ和对应标签（由额外采样得到）进行有监督回归。

### 3. 实验设计：数据集/场景、benchmark、对比方法
- **数据集**（三种不同推理类型）：
  - **FEVEROUS**：维基百科事实核查（多选，A/B/C）
  - **RuleTaker**：合成逻辑推理（真/假）
  - **TreeCut**：数学应用题（数值或“未知”）
- **目标LRM**：主要使用OLMo-3-7B-Think（完全开放，可验证训练数据无污染）；额外使用Qwen3.5-2B验证跨模型泛化。
- **基准对比方法**：
  - **朴素读者（Naive Readers）**：GPT-5.4和Claude Opus-4.6，阅读相同轨迹并直接预测（未经任务训练，仅通过提示）。
  - **单点探测（Single-location probes）**：
    - 重跑一致性：答案log-likelihood
    - 反事实敏感性：最终答案注意力权重
  - **随机预测基线**。
- **评估指标**：主指标为**Spearman相关系数**（仅关心排序，不关心绝对值），辅以Pearson。进行了**聚类自举置信区间**和**配对置换检验**。

### 4. 资源与算力
- 文中明确说明：所有行为预测器在**4块NVIDIA H200 GPU上训练，时间不超过24小时**。
- 训练数据生成阶段需要多次采样目标LRM（每样本10次重跑、10次反事实扰动），但**推理阶段成本极低**：行为预测器一次前向传播成本小于朴素读者（GPT-5.4/Claude Opus）的**1/10,000**。

### 5. 实验数量与充分性
- **实验分组**（约6组主要实验）：
  1. **主试验**（Section 4.2）：在三个数据集聚合测试集上，行为预测器 vs 两个朴素读者 + 单点探测 + 随机基线（两个目标LRM）。统计学显著性检验（p < 0.05）支持优势。
  2. **同族泛化**（Table 1）：在FEVEROUS内，用非数值子集训练，零次泛化到数值子集。
  3. **跨数据集迁移**（Table 2）：用两个数据集训练，在第三个数据集上微调（最多600步），对比源模型与微调后模型。
  4. **跨行为目标迁移**（Table 3）：从反事实敏感性迁移到提示敏感性（hint sensitivity），对比直接训练、迁移训练和朴素读者。
  5. **消融实验**（Table 4）：变动输入排列（P、R、A顺序）、初始化（随机 vs LRM权重）、骨干是否冻结。
  6. **附加控制**（附录C.3）：目标LRM自身作为朴素读者（OLMo读自己轨迹），表现远弱于行为预测器。
- **充分性评估**：实验覆盖多种任务、多种模型、泛化场景、统计检验，比较充分。但OOD（out-of-distribution）任务泛化测试有限（仅限三个相似推理数据集），论文亦承认此局限。

### 6. 论文的主要结论与发现
- **行为预测可学习**：训练后的行为预测器在重跑一致性和反事实敏感性任务上，**准确性显著高于两个前沿LLM作为朴素读者**（Spearman更高，且统计显著），同时成本极低。
- **推理轨迹包含超越朴素阅读的信息**：即使对同样的轨迹，行为预测器能提取出朴素读者无法可靠获取的模式，证明轨迹中隐藏着关于未来行为的有用信号。
- **关键设计要素**：
  - 从目标LRM初始化骨干权重是必要的（优于随机初始化）
  - 端到端微调整体骨干（而非仅训练头部）大幅提升性能
  - 任务特定输入排列重要（反事实敏感性需要prompt-echo，重跑一致性需要P–R–A）
- **泛化潜力**：行为预测可迁移到同族未见变体（零样本）和不同数据集（少量微调），甚至可迁移到新的行为属性（如提示敏感性）。

### 7. 优点
- **绕过解释不忠实问题**：不依赖人类可读的推理链，直接学习预测，避免了LRM推理轨迹自然语言表达不可靠的根本缺陷。
- **数据高效且自动化**：标签完全通过查询目标LRM自动生成，无需人类标注或领域知识。
- **推理极低开销**：一次前向传播即可完成预测，适合在线部署（如决定是否弃权、标记、路由等）。
- **实验设计严谨**：包含统计显著性检验、聚类自举、配对置换检验，对比了多种基线（朴素读者、单点探测、随机基线），并进行了多维度泛化测试和消融分析。
- **跨模型验证**：不仅限于OLMo，在Qwen3.5上也验证了结论。

### 8. 不足与局限（包括实验覆盖、偏差风险、应用限制）
- **OOD任务泛化未充分验证**：只测试了同类型推理数据集间的迁移，未测试与训练任务显著不同的行为属性（如“模型是否会拒绝回答”等）。论文自身承认这是一个局限。
- **数据生成成本高**：训练标签需要大量额外采样（每样本10+次LM生成），对于超长轨迹模型（如一些LRM）可能非常昂贵。虽然推理廉价，但初始标签收集可能不可持续。
- **未来LRM可能缩小差距**：如果未来推理模型产生更忠实的推理链（如通过显式训练），朴素阅读的准确率可能提升，从而缩小与训练行为预测器的差距。
- **偏差风险**：训练标签来自同一目标LRM的自身采样，可能引入循环偏差（模型学习到的是自身行为的模式，若模型未来行为变化需要重新训练）。但论文的泛化实验（跨数据集、跨行为目标）在一定程度上缓解了此担忧。
- **应用限制**：需要事先为每个目标LRM收集训练数据，对快速迭代的模型可能不适用。此外，三种数据集规模有限（每个约2000样本），更大规模部署可能需要更广泛的训练覆盖。

---
（完）
