---
title: Enhancing AI Interpretability and Safety through Localised Architectures
title_zh: 通过局部化架构增强人工智能的可解释性和安全性
authors: "Ian Seet, Jonas Bozenhard, Simon Ostermann"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.07998v2"
tags: ["query:hai"]
score: 8.0
evidence: 用于可解释性的局部化架构
tldr: 当前大型生成式AI模型存在可解释性差、安全性低和能耗高的问题。本文提出局部化机器学习架构，通过降低连接带宽并提高节点表达力，使模型更可解释且计算高效。实验表明，该架构在中小规模数据集上性能与深度网络相当，同时显著提升透明度和效率。该研究为开发安全、可信的AI系统提供了硬件-算法协同设计的新方向。
source: arxiv
selection_source: fresh_fetch
motivation: 大型AI模型可解释性和安全性不足，局部化架构有望在保持性能的同时提升透明度和能效。
method: 提出低带宽、高节点表达力的局部化架构，评估多种硬件ML范式（如存算一体）的适用性。
result: 局部化架构在中小数据集上性能与深度网络持平，且解释性和能效显著更优。
conclusion: 局部化架构是构建安全可解释AI的可行方向，需进一步验证其在硬件上的实际效益。
---

## 摘要
生成式人工智能的最新进展，尤其是强大的大型语言模型（LLM）和大型推理模型（LRM），引发了对这些庞大且不透明的人工智能模型的可解释性、安全性和可持续性的担忧。此类架构的强大之处不仅源于深度神经网络的可扩展性，还源于GPU集群等大规模并行硬件。深度神经网络的扩散性质使其在提供足够训练数据时具有强大的函数逼近能力，但也在可解释性和计算效率方面付出了代价。观察到局部化机器学习模型在小数据集上往往比深度神经网络更具可解释性和计算效率，我们通过类比推断，类似的优势可能适用于特定的局部化硬件机器学习架构。我们认为，具有较低带宽但每个节点更高表达能力的局部化架构，在根本上有可能比在GPU集群上运行的深度神经网络更具可解释性，同时在较小的数据集上仍保持竞争力。然后，我们评估了各种硬件机器学习范式在实现此类局部化架构方面的适用性，并评估了其每个节点的表达能力、能源效率以及所需技术的实际成熟度。

## Abstract
Recent advances in generative AI, especially powerful Large Language Models (LLMs) and Large Reasoning Models (LRMs), raise concerns over the interpretability, safety and sustainability of these large and opaque AI models. The power of such architectures is derived not only from the scalability of deep neural networks, but also massively parallel hardware such as GPU clusters. The diffuse nature of deep neural networks gives them great function-approximation capability when provided with sufficient training data but imposes a cost in interpretability and computational efficiency. Observing that localised machine learning (ML) models tend to be more interpretable and computationally efficient than deep neural networks on small datasets, we reason by analogy that similar advantages may apply to specific localised hardware ML architectures. We argue that localised architectures with lower bandwidth but higher expressivity per node have the potential to be fundamentally more interpretable than deep neural networks running on GPU clusters while remaining competitive for smaller datasets. We then evaluate the suitability of various hardware ML paradigms for implementing such localised architectures and evaluate their per-node expressivity, energy efficiency and practical maturity of the technology required.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前以大型语言模型（LLM）和大规模推理模型（LRM）为代表的深度神经网络，虽然性能强大，但存在三个关键缺陷：**可解释性差**（特别是机械可解释性不足）、**安全性低**（难以控制和对齐人类价值观）、**能耗高**（训练和推理需要大量计算资源，带来环境和成本问题）。
- **整体含义**：作者指出，这些缺陷的根本原因之一是深度神经网络的**高度分布式、高带宽互联**特性，导致特征叠加（superposition）和计算效率低下。因此，转向**局部化架构**（localised architectures）——即每个节点具有更高表达能力、节点间带宽较低——有望在保持竞争力（尤其是在中小规模数据集上）的同时，显著提升模型的可解释性和能效，从而构建更安全、更可持续的AI系统。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过类比可解释的传统机器学习模型（如广义加性模型GAMs、决策树）与深度神经网络的结构差异，提出**局部化是机械可解释性的关键**。局部化意味着模型内部的高阶特征交互被最小化，功能模块界限清晰，易于分解和定位。
- **关键技术细节**（以概念性框架为主，未给出具体算法）：
  - 理想化的“可解释决策树”扩展：在节点分裂时，允许使用各种离散/连续函数（包括高阶交互项），但训练时对高阶项施加成本，从而在保证可解释性的前提下提升表达能力。但这种模型搜索空间巨大，不适用于当前硬件。
  - 受生物神经系统启发：生物神经网络虽然带宽低、延迟高，但每个神经元（节点）具有极高的表达力（如树突计算），因此依然能完成复杂任务。人工局部化架构应模仿这一特性，用**高节点表达力**来补偿**低通信带宽**。
  - 评估四种硬件ML范式实现局部化架构的潜力：
    1. **电子模拟逻辑**：成熟但表达力不足，对噪声敏感，可解释性受损。
    2. **电化学神经形态架构**：类脑但难以精确建模到纳米尺度，速度慢。
    3. **纯化学扩散架构**：更慢、更难建模，且无证据表明能有效工作。
    4. **分子机械逻辑**：易于用牛顿力学精确模拟，表达力高、能效高，但目前无法实际合成。
- **公式/算法流程**：文中无具体公式或算法；主要定性讨论。

### 3. 实验设计

- **论文性质**：本文属于**概念论证与综述性观点论文**，未设计任何计算机实验或实际硬件实现。
- **使用的数据集/场景**：未涉及。论文仅通过类比GAMs、决策树与深度神经网络在已有文献中的表现（如对表格数据的性能对比）来支持论点。
- **Benchmark**：无。对比方法是理论层面的：将局部化架构与传统深度神经网络（运行在GPU集群上）进行可解释性、计算效率、能效等方面的定性比较。
- **对比方法**：未进行实证对比。

### 4. 资源与算力

- **明确说明**：论文未提及任何训练或推理所使用的GPU型号、数量、训练时长等具体算力资源。
- **原因**：论文不涉及实验，因此无此项信息。

### 5. 实验数量与充分性

- **实验数量**：零组实验。论文完全基于理论分析、已有文献引用和逻辑推理。
- **充分性/客观性**：论文在论证局部化架构优势时，引用了大量前人工作（如GAMs、决策树、SHAP、叠加理论、因果抽象等），逻辑链条较完整。但**缺乏任何形式的实证验证**（包括模拟或小规模原型），使得结论的充分性和公平性均受显著限制。特别是，“局部化架构在中小数据集上可与深度网络竞争”这一核心论断，仅由类比得出，无直接证据支持。

### 6. 论文的主要结论与发现

- **主要结论**：
  1. **局部化架构（低带宽、高节点表达力）在根本上比分布式深度神经网络更可解释**，且若能补偿性能损失，有望提升AI安全性。
  2. **物理硬件架构深刻影响了ML模型的选择**：高带宽低延迟的GPU集群鼓励了分布式、不可解释的模型；而低带宽硬件（如生物神经系统）自然导向局部化模型。
  3. **四种候选硬件范式中，分子机械逻辑最有潜力**（高表达力、易模拟、高能效），但当前离实际合成最远；电化学/化学系统难以精确建模；电子模拟逻辑表达力不足。
  4. **可解释性不是实现可控性的唯一途径**：即使缺乏完全机械可解释性，通过激活操控（activation steering）等方法也能实现一定程度的可控性（controllability），这同样能提升安全性。

### 7. 优点

- **视角新颖**：从**硬件-算法协同演化**的角度探讨可解释性，将局部化硬件架构作为提升可解释性的根本手段，跳出了单纯的软件改进思路。
- **系统性评估**：对电子、电化学、化学、分子机械四种硬件范式进行了全面的优缺点比较，涵盖表达力、能效、成熟度等多个维度，为未来研究提供了清晰的路线图。
- **概念明确**：清晰区分了**机械可解释性**、**事后可解释性**和**可控性**，并强调了前两者在AI安全中的不同作用。
- **引用充分**：大量引用前沿文献（如叠加理论、因果抽象、激活操控等），论证逻辑严密。

### 8. 不足与局限

- **缺乏实验验证**：整篇论文无任何实验或模拟数据，核心论点（如局部化架构的性能竞争力）仅基于类比和理论推断，说服力不足。
- **应用范围模糊**：声称局部化架构“对较小数据集保持竞争力”，但未定义“较小”的规模，也未讨论在大规模、非表格数据（如图像、自然语言）上的可行性。
- **硬件可行性低**：最被看好的分子机械逻辑目前无法合成，电化学和化学方案建模困难且速度慢，电子模拟逻辑表达力有限——所有方案均面临重大工程障碍。
- **忽略现有局部化方法**：论文未讨论已有的局部化神经网络变体（如稀疏连接、模块化网络、胶囊网络等）或硬件加速器（如存算一体芯片）的相关工作，使得论证略显绝对。
- **偏见风险**：作者对“局部化天然可解释”的预设较强，未充分考虑局部化可能带来的其他问题（例如，局部化可能导致功能碎片化、全局信息丢失等）。

（完）
